// Brendan Fattig
// function named mainif
// identifier named type

main
// errors... assignment outside of main scope / in declarations
bool trues = true;
int type = -10;

function mainif (int three, bool fasle) : void {
   if (type == 0.11) then
        return 0;
   fi;
    call mainif(type, trues );
} // error missing semicolon here

{ 
    return trues;
}.
